import React from 'react';

const ImageBox = ({ selectedImage }) => {
  return (
    <div className="image-box">
      {/* <img src={selectedImage} alt="Selected Image" /> */}
    </div>
  );
};

export default ImageBox;
